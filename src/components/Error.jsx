const Error = ({children}) => {
  return (
    <div className='bg-rose-100 text-rose-500 text-center p-3 uppercase font-bold mb-3 rounded-md'>
      {children}
    </div>
  );
};

export default Error;
